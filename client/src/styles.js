import {makeStules} from '@material-ui/core/styles';
export default makeStyles(() =>({
    appBar:{
        borderRadius: 15,
        marrgin: '30px',
        display:'flex',
        flexDirection: 'column',
        alignItems:'center',
    }
}))